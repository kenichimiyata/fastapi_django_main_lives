    1  python OpenInterpreter.py 
    2  git clone https://github.com/Eladlev/AutoPrompt.git
    3  cd ..
    4  git clone https://github.com/Eladlev/AutoPrompt.git
    5  cd ./AutoPrompt/
    6  pip install -r requirements.txt 
    7  python run_pipeline.py
    8  rm -rf .git
    9  python run_pipeline.py
   10  python run_pipeline.py     --prompt "この映画レビューにはネタバレが含まれていますか？YesまたはNoで答えてください"     --task_description "アシスタントは、映画レビューを分類するエキスパートであり、レビューされた映画のネタバレが含まれているかどうかをユーザーに教えてくれます。"
   11  python run_pipeline.py     --prompt "この映画レビューにはネタバレが含まれていますか？YesまたはNoで答えてください"     --task_description "アシスタントは、映画レビューを分類するエキスパートであり、レビューされた映画のネタバレが含まれているかどうかをユーザーに教えてくれます。"
   12  python run_pipeline.py
   13  python run_pipeline.py     --prompt "Does this movie review contain a spoiler? answer Yes or No"     --task_description "Assistant is an expert classifier that will classify a movie review, and let the user know if it contains a spoiler for the reviewed movie or not."     --num_steps 30
   14  python run_pipeline.py     --prompt "Does this movie review contain a spoiler? answer Yes or No"     --task_description "Assistant is an expert classifier that will classify a movie review, and let the user know if it contains a spoiler for the reviewed movie or not."     --num_steps 1
   15  python run_pipeline.py     --prompt "Does this movie review contain a spoiler? answer Yes or No"     --task_description "Assistant is an expert classifier that will classify a movie review, and let the user know if it contains a spoiler for the reviewed movie or not."     --num_steps e
   16  python run_pipeline.py     --prompt "Does this movie review contain a spoiler? answer Yes or No"     --task_description "Assistant is an expert classifier that will classify a movie review, and let the user know if it contains a spoiler for the reviewed movie or not."     --num_steps 4
   17  #python run_pipeline.py     --prompt  Answer Yese ore No  e   --Task_Description Assistant Is An Expert Classifier That Will classify a movie review, and let the user know if it contains a spoiler for the reviewed movie or not.     --num_steps 4
   18  python run_pipeline.py 
   19  python run_pipeline.py     --prompt "この映画レビューにはネタバレが含まれていますか？YesまたはNoで答えてください"     --task_description "アシスタントは、映画レビューを分類するエキスパートであり、レビューされた映画のネタバレが含まれているかどうかをユーザーに教えてくれます。"
   20  ython run_pipeline.py --prompt "この映画レビューにはネタバレが含まれていますか？YesまたはNoで答えてください" --task_description "アシスタントは、映画レビューを分類するエキスパートであり、レビューされた映画のネタバレが含まれているかどうかをユーザーに教えてくれます"
   21  python run_pipeline.py --prompt "この映画レビューにはネタバレが含まれていますか？YesまたはNoで答えてください" --task_description "アシスタントは、映画レビューを分類するエキスパートであり、レビューされた映画のネタバレが含まれているかどうかをユーザーに教えてくれます"
   22  python run_pipeline.py     --prompt "Does this movie review contain a spoiler? answer Yes or No"     --task_description "Assistant is an expert classifier that will classify a movie review, and let the user know if it contains a spoiler for the reviewed movie or not."     --num_steps 3
   23  python prompt.py 
   24  cd ..
   25  git add *
   26  git commit -m "update"
   27  git push -f
   28  git add *
   29  git commit -m "update"
   30  git push -f
   31  git add .gitattributes 
   32  git commit -m "update"
   33  git push -f
   34  git lfs track "*.gif"
   35  git add .gitattributes
   36  git add AutoPrompt/docs/autoprompt_recording.gif
   37  git commit -m "Track gif files with Git LFS"
   38  git push -f
   39  git push origin main
   40  git lfs track "*.gif"
   41  git add AutoPrompt/docs/autoprompt_recording.gif
   42  git commit -m "Track gif files with Git LFS"
   43  git push origin main
   44  git lfs migrate import --include="*.gif"
   45  git commit -m "Track gif files with Git LFS"
   46  git push origin main
   47  history > hist20240601.txt
